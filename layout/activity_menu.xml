<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/mail_screen_button"
        android:layout_width="151dp"
        android:layout_height="49dp"
        android:layout_marginStart="44dp"
        android:layout_marginTop="44dp"
        android:onClick="goMailScreen"
        android:text="MAIL SCREEN"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/user_list_screen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="44dp"
        android:onClick="goUserList"
        android:text="USER LIST"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/mail_screen_button" />

    <Button
        android:id="@+id/user_preferences_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="64dp"
        android:onClick="goUserPreferences"
        android:text="User Preferences"
        app:layout_constraintEnd_toEndOf="@+id/mail_screen_button"
        app:layout_constraintStart_toStartOf="@+id/mail_screen_button"
        app:layout_constraintTop_toBottomOf="@+id/mail_screen_button" />
</androidx.constraintlayout.widget.ConstraintLayout>